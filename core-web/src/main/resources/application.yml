spring:
    application:
        name: core-web
    mvc:
        view:
            prefix: /WEB-INF/jsp/
            suffix: .jsp

server:
    port: 8888

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
        healthcheck:
            enabled: true
        lease:
            duration: 5

welcome:
    message: AFT kicks butts
